services:
  gateway:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: openfabric-gateway
    ports:
      - "8310:8310"
    volumes:
      - ..:/opt/src
    restart: unless-stopped

networks:
  default:
    name: openfabric-network
